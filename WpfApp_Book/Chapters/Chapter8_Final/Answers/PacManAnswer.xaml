<Page x:Class="WpfApp_Book.Chapters.Chapter8_Final.Answers.PacManAnswer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Ответ: Pac-Man"
      Focusable="True" Loaded="Page_Loaded" Unloaded="Page_Unloaded" KeyDown="Page_KeyDown">

    <Grid Background="#000000">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#1a1a1a" Padding="15">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🎮 PAC-MAN" FontSize="24" FontWeight="Bold" Foreground="#F1C40F"/>
                    <TextBlock Text=" — Финальное задание" FontSize="16" Foreground="#666" VerticalAlignment="Bottom" Margin="10,0,0,3"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <TextBlock Text="ОЧКИ: " Foreground="#95A5A6" FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ScoreText" Text="0" Foreground="#F1C40F" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock Text="   ЖИЗНИ: " Foreground="#95A5A6" FontSize="14" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    <TextBlock x:Name="LivesText" Text="❤❤❤" Foreground="#E74C3C" FontSize="18" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Игровое поле -->
        <Border Grid.Row="1" Background="#000" Margin="10">
            <Canvas x:Name="GameCanvas" Background="#000" ClipToBounds="True"/>
        </Border>
        
        <!-- Панель управления -->
        <Border Grid.Row="2" Background="#1a1a1a" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="StartButton" Content="▶ СТАРТ" Width="100" Height="35" 
                            Background="#27AE60" Foreground="White" FontWeight="Bold"
                            Margin="0,0,10,0" Click="Start_Click"/>
                    <Button Content="↻ ЗАНОВО" Width="100" Height="35" 
                            Background="#E74C3C" Foreground="White" FontWeight="Bold"
                            Click="Reset_Click"/>
                </StackPanel>
                
                <TextBlock Grid.Column="1" x:Name="StatusText" Text="Нажмите СТАРТ!" 
                           Foreground="#F1C40F" FontSize="16" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                
                <TextBlock Grid.Column="2" Text="↑↓←→ или WASD" 
                           Foreground="#666" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Page>
